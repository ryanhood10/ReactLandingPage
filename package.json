{
  "homepage": "ryans-portfolio.herokuapp.com",
  "name": "ryans-portfolio",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nodemailer": "^6.9.1"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd shellproject && npm install && npm run build"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  },
  "engines": {
    "node": "14.x"
  }
}
